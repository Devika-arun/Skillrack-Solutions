#include <stdio.h>
#include <stdlib.h>

int main() {
  int n, x;
  scanf("%d %d", &n, &x);
  int limit = n/x;
  int i=1, j=limit;
  while (i <= limit/2 && j >= limit/2) {
    printf("%d %d", i*x, j*x);
    i += 1
    j -= 1
  }
  if (i == j) {
    printf("%d ", (lim/2 + 1)*x);
  }
  return 0;
}  
